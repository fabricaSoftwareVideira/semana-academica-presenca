<!-- src/views/dashboard.ejs -->
<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Semana Acadêmica</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
        integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="icon" href="/img/favicon.svg" type="image/svg+xml">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .ranking-list {
            display: grid;
            grid-template-columns: repeat(3, minmax(250px, 1fr));
            gap: 15px;
            margin-top: 20px;
        }

        .ranking-card {
            display: flex;
            align-items: center;
            background: white;
            padding: 15px 20px;
            border-radius: 10px;
            transition: transform 0.2s;
            border: 2px solid transparent;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.05);
        }

        /* 1º lugar */
        .ranking-card:first-child {
            border: 3px solid gold;
            box-shadow: 0 6px 15px rgba(255, 215, 0, 0.5);
            background: #fffbea;
        }

        /* 2º lugar */
        .ranking-card:nth-child(2) {
            border: 3px solid silver;
            box-shadow: 0 6px 15px rgba(192, 192, 192, 0.4);
            background: #f9f9fb;
        }

        /* 3º lugar */
        .ranking-card:nth-child(3) {
            border: 3px solid #cd7f32;
            /* bronze */
            box-shadow: 0 6px 15px rgba(205, 127, 50, 0.4);
            background: #fff5e6;
        }

        .ranking-position {
            font-size: 1.5rem;
            font-weight: bold;
            margin-right: 20px;
            min-width: 40px;
            min-height: 40px;
            text-align: center;
            background: var(--primary-color);
            color: var(--secondary-color);
            border-radius: 50%;
        }

        .ranking-info {
            display: flex;
            flex-direction: column;
            gap: 5px;
        }

        .ranking-name {
            font-weight: 600;
            font-size: 1.1rem;
            color: #333;
        }

        .ranking-points span {
            display: block;
            font-size: 0.9rem;
            color: #666;
            text-align: right;
        }

        .ranking-points .total {
            font-weight: bold;
            color: var(--primary-color);
            font-size: 1rem;
        }

        .legenda {
            margin-top: 20px;
            background: var(--secondary-color);
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.05);
            font-size: 0.85rem;
        }

        .legenda h3 {
            margin-bottom: 10px;
            color: var(--primary-color);
        }

        .legenda ul {
            list-style-type: disc;
            padding-left: 20px;
            color: #555;
        }

        /* Responsividade */
        @media (max-width: 1024px) {
            .ranking-list {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        /* Mobile */
        @media (max-width: 480px) {
            .ranking-list {
                grid-template-columns: 1fr;
            }

            .ranking-points span {
                font-size: 0.85rem;
            }
        }

        /* Container de vitórias */
        .vitorias-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 10px;
            margin-bottom: 40px;
        }

        .vitorias-turma-card {
            background: #fefefe;
            border-radius: 10px;
            padding: 15px 20px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
            transition: transform 0.2s, box-shadow 0.2s;
        }

        .vitorias-turma-card h3 {
            margin-top: 0;
            margin-bottom: 10px;
            font-size: 1.2rem;
        }

        .vitorias-turma-card ul {
            padding-left: 18px;
            list-style-type: disc;
            color: #555;
        }

        .vitorias-turma-card ul li {
            font-size: 0.9rem;
            line-height: 1.4;
        }

        /* Responsividade */
        @media (max-width: 768px) {
            .vitorias-container {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>

<body>
    <nav>
        <h1>
            <a href="/" class="logo">
                <img src="/img/ifc.webp" alt="Logo" class="logo-img">
                Semana Acadêmica
            </a>
        </h1>
        <div class="user-info">
            <% if (typeof user !=='undefined' ) { %>
                <div class="floating-btn" id="topBtn" title="Registrar Participação"
                    onclick="window.location.href='/participacao'">
                    <i class="fa fa-plus"></i>
                </div>
                <span id="userName">
                    <%= typeof user !=='undefined' ? user.username : 'Usuário' %>
                        (<%= typeof user !=='undefined' ? user.role : '' %>)
                </span>
                <a href="/auth/logout" class="logout-btn">Sair</a>
                <% } else { %>
                    <a href="/auth/login" class="login-btn">Login</a>
                    <% } %>
        </div>
    </nav>

    <header>
        <div class="container">
            <div class="breadcrumb">
                <a href="/">Início</a> &gt; <span>Ranking</span>
            </div>
        </div>
    </header>

    <div class="container">
        <h2 class="page-title">Ranking de Participações</h2>
        <div class="ranking-list">
            <% ranking.forEach((item, index)=> { %>
                <div class="ranking-card">
                    <div class="ranking-position">
                        <%= index + 1 %>
                    </div>
                    <div class="ranking-info">
                        <div class="ranking-name">
                            <%= item.nome %>
                        </div>
                        <div class="ranking-points">
                            <span>Pontos Vitórias: <%= item.pontosVitorias %></span>
                            <span>Pontos Alunos: <%= item.pontosTotalAlunos %></span>
                            <span class="total">Total: <%= item.pontosTotal %></span>
                        </div>

                    </div>
                </div>
                <% }) %>
        </div>

        <div class="legenda">
            <h3>Legenda:</h3>
            <ul>
                <li><strong>Pontos Vitórias:</strong> Pontos da turma acumulados por vitórias em competições.</li>
                <li><strong>Pontos Alunos:</strong> Pontos acumulados por participação de alunos nas atividades.</li>
                <li><strong>Total:</strong> Soma dos pontos de vitórias em equipe e pontos de alunos.</li>
            </ul>
        </div>
    </div>

    <div class="container">
        <h2 class="page-title">Vitórias por Turma</h2>
        <div class="vitorias-container">
            <% ranking.forEach(item=> { %>
                <div class="vitorias-card">

                    <div class="vitorias-turma-card">
                        <h3>
                            <%= item.nome %>
                        </h3>
                        <% if (item.vitorias.length> 0) { %>
                            <ul>
                                <% item.vitorias.forEach(v=> {
                                    let medalha = '';
                                    if(v.posicao === 1) medalha = '🥇';
                                    else if(v.posicao === 2) medalha = '🥈';
                                    else if(v.posicao === 3) medalha = '🥉';
                                    %>
                                    <li>
                                        <%= medalha %>
                                            <%= v.eventoNome %> - <%= v.posicao %>º lugar (+<%= v.pontos %> pts)
                                    </li>
                                    <% }) %>
                            </ul>
                            <% } else { %>
                                <p>Sem vitórias registradas.</p>
                                <% } %>
                    </div>
                </div>
                <% }) %>
        </div>

    </div>


    <footer role="contentinfo">
        <div class="footer-content">

            <!-- Informações do site -->
            <div class="footer-info">
                <h3>Semana Acadêmica</h3>
                <div class="footer-logo">
                    <img src="/img/favicon.svg" alt="Fábrica de Software" class="footer-logo">
                    <img src="/img/ifc.webp" alt="IFC Videira" class="footer-logo">
                </div>

            </div>

            <!-- Redes sociais -->
            <div class="footer-social">
                <h3>Siga-nos</h3>
                <ul>
                    <li><a href="https://www.instagram.com/fsw.ifcvideira/" target="_blank"><i
                                class="fab fa-instagram"></i></a></li>
                    <li>
                        <a href="mailto:fabricadesoftware.videira@ifc.edu.br" target="_blank"><i
                                class="fa fa-envelope"></i>
                        </a>
                    </li>
                </ul>
            </div>

        </div>
    </footer>

</body>

</html>