<!-- src/views/dashboard.ejs -->
<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
        integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
    <title>Dashboard - Semana Acadêmica</title>
    <link rel="icon" href="/img/favicon.svg" type="image/svg+xml">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>

    <nav>
        <h1>
            <a href="/" class="logo">
                <img src="/img/ifc.webp" alt="Logo" class="logo-img">
                Semana Acadêmica
            </a>
        </h1>
        <div class="user-info">
            <% if (typeof user !=='undefined' ) { %>
                <div class="floating-btn" id="topBtn" title="Registrar Participação"
                    onclick="window.location.href='/participacao'">
                    <i class="fa fa-plus"></i>
                </div>
                <span id="userName">
                    <%= typeof user !=='undefined' ? user.username : 'Usuário' %>
                        (<%= typeof user !=='undefined' ? user.role : '' %>)
                </span>
                <a href="/auth/logout" class="logout-btn">Sair</a>
                <% } else { %>
                    <a href="/auth/login" class="login-btn">Login</a>
                    <% } %>
        </div>
    </nav>

    <header>
        <div class="jumbotron">
            <canvas id="particle-canvas"></canvas>
            <div class="jumbotron-content">
                <!-- <h2 class="page-title">Bem-vindo, <%= typeof user !=='undefined' ? user.username : 'Visitante' %>!</h2> -->
                <h2 class="page-title">Semana Acadêmica 2025</h2>
                <p>Semana Acadêmica do Curso de Técnico em Informática do IFC - <i>Campus Videira</i> que acontecerá de
                    <br /><b>10 a 12 de novembro de 2025</b>.
                </p>
                <a href="/qrcode" class="btn btn-warning">
                    <i class="fa fa-qrcode"></i>
                    Gerar Meu QR Code
                </a>
            </div>
        </div>
    </header>

    <div class=" container">
        <!-- Mostrar a turma que está em primeiro lugar e um link para acessar o ranking completo -->
        <section class="top-ranking">
            <h2>🏆 Primeira Colocação</h2>
            <% if (rankingExistePontuacao) { %>
                <% if (ranking && ranking.length> 0) { %>
                    <p>A turma <strong>
                            <%= ranking[0].nome %>
                        </strong> está em primeiro lugar com <strong>
                            <%= ranking[0].pontosTotal %>
                        </strong> pontos!</p>
                    <a href="/ranking" class="card-btn">Ver Ranking Completo</a>
                    <% } %>
                        <% } else { %>
                            <p>Nenhuma pontuação registrada ainda. Participe dos eventos para ganhar pontos!</p>
                            <% } %>
        </section>

        <!-- Listar detalhes sobre os eventos -->
        <section class="eventos">
            <h2>📅 Programação Completa</h2>
            <% if (eventosPorTipo) { %>
                <div class="eventos-list">
                    <% eventosPorTipo.forEach(tipo=> { %>
                        <div class="eventos-item">
                            <h3 class="eventos-title">
                                <%= tipo.tipo %>
                            </h3>
                            <% if (tipo.eventos.length> 0) { %>
                                <% tipo.eventos.forEach(evento=> { %>
                                    <p class="eventos-desc"><strong>
                                            <%= evento.nome %>
                                        </strong></p>
                                    <p class="eventos-date">Data: <%= new Date(evento.data).toLocaleDateString('pt-BR')
                                            %>
                                    </p>
                                    <p class="eventos-time">Horário: <%= evento.horario %>
                                    </p>
                                    <p class="eventos-location">Local: <%= evento.local %>
                                    </p>
                                    <hr>
                                    <% }) %>
                                        <% } else { %>
                                            <p>Nenhum evento disponível para este tipo.</p>
                                            <% } %>
                        </div>
                        <% }) %>
                </div>
                <% } else { %>
                    <p>Nenhum evento disponível no momento.</p>
                    <% } %>
        </section>

    </div>

    <footer>
        <div class="footer-content">

            <!-- Informações do site -->
            <div class="footer-info">
                <h3>Semana Acadêmica</h3>
                <div class="footer-logo">
                    <img src="/img/favicon.svg" alt="Fábrica de Software" class="footer-logo">
                    <img src="/img/ifc.webp" alt="IFC Videira" class="footer-logo">
                </div>

            </div>

            <!-- Redes sociais -->
            <div class="footer-social">
                <h3>Siga-nos</h3>
                <ul>
                    <li><a href="https://www.instagram.com/fsw.ifcvideira/" target="_blank"><i
                                class="fab fa-instagram"></i></a></li>
                    <li>
                        <a href="mailto:fabricadesoftware.videira@ifc.edu.br" target="_blank"><i
                                class="fa fa-envelope"></i>
                        </a>
                    </li>
                </ul>
            </div>

        </div>
    </footer>

    <script>
        const canvas = document.getElementById("particle-canvas");
        const ctx = canvas.getContext("2d");
        const jumbotron = document.querySelector(".jumbotron");

        function resizeCanvas() {
            canvas.width = jumbotron.offsetWidth;
            canvas.height = jumbotron.offsetHeight;
        }
        window.addEventListener("resize", resizeCanvas);
        resizeCanvas();

        const emojis = ["💻", "🚀", "🤖", "🎮", "🕹️", "📱", "✨", "🧙‍♂️", "🧝‍♀️", "🧛", "🦸‍♂️", "🦹‍♀️"];

        let particles = Array.from({ length: 20 }, () => ({
            x: Math.random() * canvas.width,
            y: Math.random() * canvas.height,
            dx: (Math.random() - 0.5) * 1.2,
            dy: (Math.random() - 0.5) * 1.2,
            emoji: emojis[Math.floor(Math.random() * emojis.length)],
            size: Math.random() * 14 + 14 // tamanho do emoji
        }));

        function drawParticles() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            particles.forEach(p => {
                ctx.font = `${p.size}px Arial`;
                ctx.fillText(p.emoji, p.x, p.y);

                p.x += p.dx;
                p.y += p.dy;

                if (p.x < 0 || p.x > canvas.width) p.dx *= -1;
                if (p.y < 0 || p.y > canvas.height) p.dy *= -1;
            });
            requestAnimationFrame(drawParticles);
        }
        drawParticles();
    </script>


</body>

</html>