<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <title>Login - Semana Acadêmica</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            font-family: Arial;
            background: #f7f7f7;
        }

        .login-box {
            background: #fff;
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
            width: 90%;
            max-width: 400px;
        }

        h1 {
            margin-bottom: 20px;
        }
    </style>
</head>

<body>
    <div class="login-box">
        <h2>Login</h2>
        <% if (typeof error !=='undefined' ) { %>
            <p style="color: red;">
                <%= error %>
            </p>
            <% } %>
                <form action="/auth/login" method="POST">
                    <div>
                        <label>Usuário:</label>
                        <input type="text" name="username" required value="fabricio.bizotto">
                    </div>
                    <div>
                        <label>Senha:</label>
                        <input type="password" name="password" required value="admin123">
                    </div>
                    <button type="submit">Entrar</button>
                </form>
    </div>

</body>

</html>